node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
# $env/static/public needs these declared even when empty, so an unconfigured build
# (no .env.local) still has exports to resolve — see src/lib/auth/session.ts.
!.env
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
.DS_Store
.claude/settings.local.json
.claude/scheduled_tasks.lock
